# Development workflow

- Reuse and monitor an existing background `npm run dev` process for routine verification.
- Verify changes against the live development site and its process/log output.
- Do not run `npm run build` unless the user explicitly requests a production build.
