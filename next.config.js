/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  images: {
    domains: ['i.scdn.co']
  }
}

module.exports = nextConfig
module.exports = {
  images: {
    domains: ['i.scdn.co'],
  },
}
